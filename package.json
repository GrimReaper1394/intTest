{
  "name": "ilumn",
  "version": "1.0.0",
  "description": "react infinite slider",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "npm start --prefix client",
    "clientinstall": "npm install --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tich",
  "license": "ISC",
  "devDependencies": { 
    "concurrently": "^5.0.2",
    "nodemon": "^2.0.2"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-env-run": "^3.0.2",
    "npm-run-all": "^4.1.5"
  }
}
